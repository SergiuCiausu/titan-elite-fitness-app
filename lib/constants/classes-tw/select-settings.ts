import { h3Settings } from "./h3-settings";

export const selectSettings = {
    select: `outline-none bg-transparent ${h3Settings} !text-xl 3xl:!text-2xl font-body font-semibold pr-2`,
    option: `bg-background text-base 3xl:text-xl outline-primary outline-[2px] text-primary-background`,
}